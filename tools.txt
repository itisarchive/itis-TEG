python 3.13
dotenv
built-in python packages
common python packages (numpy, sklearn, pandas, matplotlib, seaborn etc.)
AzureOpenAI and AzureChatOpenAI, all available models
100$ in Azure credits
system prompts
user prompts
temperatures
max_completion_tokens
top_p
streamlit

embeddings
text-embedding-3-small
langchain
OllamaLLM
ChatPromptTemplate

RAGs
AzureOpenAIEmbeddings, all available models
InMemoryVectorStore
DirectoryLoader
rag_chains (with retrievers, RunnablePassthrough, prompt, llm and StrOutputParser)
RecursiveCharacterTextSplitter (chunking)
Chroma vector stroe
FAISS vector store
TextLoader
PyPDFLoader
WebBaseLoader
requests
BeautifulSoup
User-Agent with ordinary headers
metadata_filtering (storing document/chunk embeddings with metadata for filtered retrieval)
hybrid search (vector + BM25 + TF-IDF, combinations, adaptive)
query expansion: by synonyms, context keywords, by llm (with approaches, contexts, context-aware)
re-ranking of documents (by llms, by cross encoders such as MS-MARCO MiniLM and NLI Electra)
RAGAS (using expert LLM to generate expert truths, evaluating RAG using another LLM)
Neo4J
Faker
docker
LLMGraphTransformer
Neo4jGraph
graph db with ontology
GraphCypherQAChain

